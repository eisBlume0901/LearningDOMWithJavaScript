JavaScript (CRUD) - DOM Manipulation
- change HTML elements/attributes
- change CSS styles
- remove existing HTML elements/attributes
- add new HTML elements/attributes
- create new HTML events

HTML DOM - get, change, add, and delete HTML elements (CRUD)